# Дополнение к отчету от 24.07.2026

Основание: письмо АП-Риал `Re: Итог по исправлениям сайтов от 22.07.2026` от 24.07.2026 и семь приложенных файлов с замечаниями.

## Выполненные работы

### apreal.spb.ru

- Стабилизирована форма `Перезвонить` в инфографике.
- Абсолютное позиционирование полей и кнопки заменено устойчивой сеткой.
- Имя, телефон и кнопка выровнены и больше не перекрываются.
- Форма отправляет данные через действующий обработчик сайта.

### license39.ru

- Стабилизирована форма `Перезвонить` в инфографике.
- Поля и кнопка переведены на сеточную раскладку без абсолютных координат.
- Проверена отправка через действующий обработчик.
- Появление писем с указанием Beget является нормальным после переноса сайта на этот хостинг.

### apreal-nn.ru

- Исправлено наложение полей и кнопки формы `Перезвонить`.
- Сброшены унаследованные абсолютные координаты и внешние отступы.
- Проверена отправка через действующий обработчик.

### apreal.ru

- Поля имени и телефона и кнопка выровнены по одной линии.
- Добавлена устойчивая сетка и аккуратный внутренний отступ от рамки.

### shopap.ru

- В форму `ЗАДАТЬ ВОПРОС` возвращены имя, телефон и поле вопроса.
- Добавлена видимая проверка `Введите цифрами: пять`.
- Серверный обработчик снова передает имя и текст вопроса в письмо.
- Обновлена версия подключения скрипта для исключения старого браузерного кеша.

### nousro-nn.ru

- Кнопка `ОТПРАВИТЬ ЗАЯВКУ` переключена со старого проблемного окна на стандартную форму.
- Заголовок окна изменен на `ОСТАВИТЬ ЗАЯВКУ`.
- Крестик находится внутри окна и корректно закрывает его.
- Убраны дублирующие закрепленные кнопки за виджетом онлайн-чата.
- Виджет чата скрывается на время открытия формы, поэтому не перекрывает ее.

## Проверка

- Перед каждым развертыванием сохранены серверные резервные копии.
- Загруженные файлы проверены по SHA-256.
- PHP-файлы на сервере прошли `php -l` без ошибок.
- Живые формы вернули HTTP 200 и штатное сообщение об успешной отправке.
- Проверены реальные отправки с `apreal.spb.ru`, `license39.ru`, `apreal-nn.ru`, `shopap.ru` и `nousro-nn.ru`: обработчики вернули HTTP 200 и штатный ответ об успешной отправке.
- Внешний вид просмотрен вручную на 1360x900, 1920x1080 и 390x844.
- Проверены открытие и закрытие модальных окон, отсутствие наложений и выходов за экран.

## Ограничение приемки

Серверы сайтов приняли тестовые обращения и вернули успешный результат. Фактическое появление каждого тестового письма во входящих адресата можно окончательно подтвердить только в почтовых ящиках клиента.

## Учет для сметы

Эти изменения являются приемочным доведением ранее выполненного пакета исправлений по формам. Отдельная новая сумма за них в этом отчете не начислялась.

## Повторная сверка переписки

- Сопоставлены входящие технические задания клиента, отправленные ответы и локальные отчеты за 15-24 июля.
- Подтверждено, что последние замечания по `apreal.spb.ru`, `license39.ru`, `apreal-nn.ru`, `apreal.ru`, `shopap.ru` и `nousro-nn.ru` перечислены в отправленном письме Gmail `19f92ee4be68dc28`.
- Найден коммуникационный пробел: завершение работ по `lfsb.ru`, `otxodi.ru` и почте `mchs-spb.ru` было зафиксировано в технических отчетах, но клиенту не направлялось отдельное итоговое подтверждение.
- `lfsb.ru` и `otxodi.ru` повторно проверены на публичных страницах в размерах `1440x900` и `390x844`: обе формы открываются и закрываются, не выходят за экран и не имеют горизонтального переполнения.
- Единственный ответ `404` на `lfsb.ru` относится к старому декоративному фону темы `/images/bg-center.jpg`; он не используется формами, не создает пустого или сломанного блока и не влияет на пользовательский сценарий.
- Выполнены четыре новые контрольные отправки: обратный звонок и вопрос на каждом из двух сайтов. Все обработчики вернули HTTP `200` и сообщение об успешной отправке.
- Повторно подтверждены публичные записи `mchs-spb.ru`: MX Beget, SPF `include:beget.com` и DKIM `beget._domainkey`; главная страница отвечает HTTP `200`.
- Клиенту направлено отдельное дополнение на `khorov69@mail.ru`, копия `upreal@bk.ru`; Gmail `19f935839b9d04c9`. В письме перечислены выполненные работы и запрошено подтверждение получения новых контрольных заявок с `lfsb.ru` и `otxodi.ru`.
- После отправки письмо повторно прочитано в Gmail: адресаты, тема, текст и метка `SENT` подтверждены.

## Текущий статус приемки

- Клиент подтвердил фактическое получение контрольных писем с `apreal.spb.ru`, `license39.ru`, `apreal-nn.ru`, `shopap.ru` и `nousro-nn.ru`.
- Клиент принял без замечаний `shopap.ru`, `nousro-nn.ru` и `apreal36.ru`.
- Дополнительные визуальные замечания по встроенным формам `apreal.spb.ru`, `license39.ru`, `apreal-nn.ru` и `apreal.ru` исправлены и проверены; подробности приведены ниже.

## Дополнительное замечание по docp.ru от 24.07.2026

- Получено письмо клиента `сайт DOCP.RU` с просьбой удалить дублирующие закрепленные кнопки, находившиеся за виджетом онлайн-чата.
- До правки проблема воспроизведена и визуально зафиксирована на экранах `1440x900` и `390x844`.
- Из стандартного компонента удален только блок закрепленных кнопок `ЗАКАЗАТЬ ЗВОНОК` и `ЗАДАТЬ ВОПРОС`.
- Штатные кнопки сайта, модальные формы и серверный обработчик сохранены.
- Перед публикацией создана серверная резервная копия `/home/n/nousroc9/_backups/20260724-123812-docp.ru-feedback.php`.
- Серверная проверка `php -l` прошла без ошибок; SHA-256 опубликованного файла: `ebbc6b2619f35a97c651bd1bf7ebc503b4cf80dd75ccdf52d8855344a56fbee6`.
- После публикации повторно просмотрены страницы и формы на `1440x900` и `390x844`: лишних кнопок нет, чат доступен, штатная кнопка открывает форму, крестик закрывает ее, ошибок консоли нет.
- Генератор форм и регрессионный тест обновлены, чтобы закрепленные кнопки не вернулись при следующем развертывании.
- Клиенту результат пока не отправлялся в соответствии с ограничением текущей автоматической проверки почты.

## Дополнительные замечания по встроенным формам от 24.07.2026

- Из последнего письма клиента и четырех документов извлечены и просмотрены все снимки состояния до и после отправки.
- На `apreal.spb.ru`, `license39.ru` и `apreal-nn.ru` воспроизведен пересчет колонок формы после появления сообщения об успехе: размеры менялись с `240 / 240 / 124 px` на `162 / 162 / 365 px`, из-за чего поля накладывались друг на друга.
- В общем генераторе формы закреплены фиксированные колонки и собственная ширина строки; сообщение об успехе вынесено в отдельную вторую строку сетки.
- На `apreal.ru` добавлен горизонтальный внутренний отступ `12 px`, поэтому первое поле больше не касается левой рамки.
- Перед публикацией скачаны и сверены текущие серверные версии файлов.
- Созданы резервные копии:
  - `/home/n/nousroc9/_backups/20260724-161304-apreal.spb.ru-feedback.php`;
  - `/home/n/nousroc9/_backups/20260724-161317-license39.ru-feedback.php`;
  - `/home/n/nousroc9/_backups/20260724-161331-apreal-nn.ru-feedback.php`;
  - `/home/n/nousroc9/_backups/20260724-161344-apreal-front-page.php`.
- Все четыре опубликованных PHP-файла прошли серверный `php -l`, а загруженные данные подтверждены по SHA-256.
- На трех региональных сайтах выполнены реальные отправки обычным нажатием кнопки без принудительного клика: серверы вернули HTTP `200` и штатное сообщение об успехе.
- Геометрия полей и кнопки измерена до и после ответа: размеры и координаты остаются неизменными, пересечений нет.
- Свежие снимки всех четырех сайтов просмотрены вручную на `1360x900`; форма, сообщение и окружающие блоки не перекрываются.
- На ширине `390x844` старая встроенная инфографика скрыта самим мобильным шаблоном. Видимые мобильные кнопки проверены обычным нажатием: стандартные формы открываются, полностью помещаются в экран и не имеют переполнения.
- Локальные регрессионные тесты дополнены проверкой стабильной сетки и внутреннего отступа `apreal.ru`.
- Клиенту результат пока не отправлялся в соответствии с ограничением текущей автоматической проверки почты.

## Повторное замечание по otxodi.ru от 24.07.2026

- В ответном письме клиент подтвердил, что `LFSB.RU` работает и контрольные заявки с `LFSB.RU` и `OTXODI.RU` приходят.
- По `OTXODI.RU` клиент сообщил, что верхняя надпись `ЗАДАТЬ ВОПРОС` не реагирует на мышь; приложенный документ содержит два снимка именно этого элемента.
- Проверка обычным кликом без принудительного обхода показала, что форма открывается, но у верхнего элемента отсутствовал курсор-указатель и визуальный отклик при наведении.
- Для верхнего элемента добавлены курсор `pointer`, аккуратное hover-состояние и видимый контур клавиатурного фокуса.
- Перед публикацией сохранена резервная копия `/home/n/nousroc9/_backups/20260724-150919-otxodi.ru-feedback.php`.
- Серверная проверка `php -l` прошла без ошибок; SHA-256 опубликованного файла: `12ca9d6f08da33a9d282efd5d75a806f8d89cbf1db6d4549ce4bd3264ad79077`.
- После публикации выполнены реальные наведение, обычный клик и нажатие `Enter` на `1440x900` и `390x844`: форма открывается и закрывается, ошибок консоли нет.
- Состояния наведения и открытой формы просмотрены вручную на свежих снимках обеих версий сайта; наложений и выходов за экран нет.
- В письме упомянут отдельный файл по `MCHS-SPB.RU`, однако Gmail содержит только одно вложение по `OTXODI.RU`; нового технического замечания по `MCHS-SPB.RU` в тексте или вложении нет.
- Клиенту результат пока не отправлялся в соответствии с ограничением текущей автоматической проверки почты.

## Дополнительное замечание по mchs-spb.ru от 24.07.2026

- В следующем письме клиент приложил отдельный документ по `MCHS-SPB.RU`: обратный звонок отображал успех, но письмо не поступало; форма вопроса содержала e-mail вместо имени и телефона.
- До правки проблема воспроизведена на живом сайте. Форма вопроса действительно содержала обязательный e-mail и не содержала телефона и имени.
- Для сайта опубликован тот же компонент форм, который клиент уже принял на `OTXODI.RU`: обратный звонок содержит необязательное имя и обязательный телефон; вопрос содержит необязательные имя и вопрос и обязательный телефон; обе формы имеют проверку цифрой `5` и согласие на обработку данных.
- Существующие верхние кнопки сайта сохранены. На мобильной версии добавлены доступные кнопки сразу под шапкой, поскольку старый шаблон скрывал все точки входа в формы.
- Перед публикацией проверены контрольные суммы текущих файлов и сохранена резервная копия `/home/n/nousroc9/_backups/20260724-173618-mchs-spb-forms`.
- Опубликованный PHP-файл прошел серверный `php -l`; SHA-256: `906d180e1d8dc0c87e3d908c497377d17a0e7e924e6beb7aa94cc415f158e95f`.
- Причина недоставки найдена в DNS: после переноса MX домена указывали на `mx1.beget.com` и `mx2.beget.com`, хотя рабочий ящик находится в VK WorkSpace.
- Полная зона сохранена перед изменением. Точечно заменены только почтовые записи корня: MX установлен в `emx.mail.ru` с приоритетом `10`, SPF изменен на `v=spf1 include:beget.com include:_spf.mail.ru ~all`. A-записи сайта, CNAME, NS и существующие DKIM-записи не удалялись.
- Публичная проверка DNS после сохранения показывает MX `emx.mail.ru`, обновленный SPF и прежний A-адрес `87.236.16.29`.
- На живом сайте обычными кликами отправлены две уникальные контрольные заявки: обратный звонок и вопрос. Оба обработчика вернули штатное сообщение об успехе.
- Выполнен вход непосредственно в рабочий ящик `info@mchs-spb.ru`; обе новые заявки найдены во входящих с правильными темами, именами, телефоном и текстом вопроса.
- Свежие состояния обеих форм и сообщения об успехе просмотрены вручную на `1360x900` и `390x844`: элементы не перекрываются, формы помещаются в экран с предусмотренной прокруткой, крестики работают, ошибок консоли нет.
- Генератор и регрессионный тест дополнены, чтобы конфигурация `MCHS-SPB.RU` не вернулась к старому варианту при следующей публикации.
- Клиенту результат пока не отправлялся в соответствии с ограничением текущей автоматической проверки почты.
