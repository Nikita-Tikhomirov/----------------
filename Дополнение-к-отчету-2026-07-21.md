# Дополнение к отчету за 21 июля 2026 года

## Проверенные письма клиента

1. Получено повторное замечание по встроенной форме обратного звонка на `apreal.ru`.
2. Получен вопрос о возможности переноса `info@mchs-spb.ru` из VK Workspace на обычную почту Beget.
3. Клиент подтвердил получение контрольной заявки с `apreal.ru`, но прислал снимок с нарушенной версткой формы.
4. Клиент передал доступ к `info@mchs-spb.ru` и подтвердил, что старые письма переносить не нужно.
5. Новых писем клиента после этих сообщений на момент повторной проверки нет.

## apreal.ru

- Установлена причина: в активной теме прежняя встроенная форма была заменена одной кнопкой, открывавшей отдельное модальное окно.
- В блок «Закажите обратный звонок» возвращены поля имени и телефона и кнопка «Перезвонить».
- Имя оставлено необязательным, телефон обязателен.
- Отправка выполняется без перехода на другую страницу.
- После успешной отправки показывается текст «Спасибо за Ваше сообщение. Оно успешно отправлено» и кнопка закрытия справа вверху.
- Получатель заявки сохранен штатный: `info@apreal.ru`. Gmail исполнителя получателем не является.
- Добавлены серверная проверка номера телефона, защита скрытым полем и ограничение частоты повторных отправок.
- Перед публикацией создана резервная копия:
  `/home/n/nousroc9/_backups/20260721-135607-apreal-inline-callback`.
- Выполнена реальная контрольная заявка с именем «Техническая проверка 21.07»; сервер вернул успешный ответ HTTP 200.
- Клиент подтвердил получение контрольной заявки.
- После замечания клиента исправлена верстка: восстановлено абсолютное позиционирование внутри исходного инфографического блока, добавлены размеры и стили для поля телефона `type="tel"`.
- Повторная визуальная проверка выполнена в чистой браузерной сессии: блок имеет размер `736 x 108`, поля имени и телефона — `240 x 42`, кнопка — высоту `46`; пересечений элементов нет.
- Повторная сквозная отправка через живую форму вернула HTTP 200 и сообщение об успешной отправке. Кнопка закрытия скрывает результат и снова показывает форму.
- Перед повторной публикацией создана резервная копия:
  `/home/n/nousroc9/_backups/20260721-145059-apreal-inline-layout/front-page.php`.
- Контрольная сумма опубликованного файла:
  `fb3724bef2985f680d2c0f46e10dd802a1ac70b4ac6c6184306c7e8b2a0e1975`.

## apreal36.ru

- Полученный от клиента DKIM-ключ добавлен в DNS Beget как TXT-запись `mailru._domainkey`.
- Публичная проверка Google DNS возвращает запись с кодом `Status: 0`; распространение DNS завершено.

## mchs-spb.ru

- Клиент подтвердил, что старые письма переносить не нужно.
- На Beget создан ящик `info@mchs-spb.ru`.
- До изменения DNS отдельно проверена авторизация: IMAP `imap.beget.com:993` и SMTP `smtp.beget.com:465` работают.
- MX домена переключены с Mail.ru на `mx1.beget.com` и `mx2.beget.com`.
- SPF приведен к `v=spf1 include:beget.com ~all`, старый проверочный TXT Mail.ru удален.
- Публичная проверка Google DNS подтверждает новые MX и SPF.
- Выполнена двусторонняя проверка доставки:
  - письмо из нового ящика принято Gmail;
  - письмо из Gmail принято новым ящиком на Beget и найдено через IMAP.
- В заголовках исходящего письма SPF имеет результат `pass`.
- Поддержка Beget включила DKIM с селектором `beget._domainkey` по тикету `2952177`.
- Выполнена повторная реальная отправка из `info@mchs-spb.ru` в Gmail. В исходных заголовках Gmail подтверждены `dkim=pass` и `spf=pass`.
- Старые DNS-алиасы `mail.mchs-spb.ru` и `webmail.mchs-spb.ru`, ведущие на Mail.ru, пока не изменялись: они не участвуют в доставке, а преждевременная замена может привести к ошибке TLS. Для веб-почты Beget используется `https://web.beget.email`.

## mhsl.ru и med-license.ru

- Формы «Задать вопрос» приведены к согласованному составу и порядку полей: необязательный вопрос, обязательный телефон, необязательное имя, капча.
- Поле email из этих форм удалено; серверные обработчики теперь требуют телефон и включают вопрос, телефон и имя в письмо.
- Исправлен `z-index` модальных окон, чтобы чат не перекрывал форму.
- Проверено отклонение заявок без телефона и успешное принятие реальных контрольных заявок.
- Выполнена визуальная проверка на desktop и в размере `390 x 844`: поля и кнопки помещаются, пересечений нет.
- Резервные копии:
  - `/home/n/nousroc9/_backups/20260721-154834-question-fields`;
  - `/home/n/nousroc9/_backups/20260721-155102-question-fields`.
- Окончательное подтверждение появления писем именно в ящиках Mail.ru требует паролей приложений: обычные пароли отклоняются IMAP с сообщением `Application password is REQUIRED`.

## fste.ru

- Старая кнопка «Подать заявку» переименована в «ЗАКАЗАТЬ ЗВОНОК» и открывает новую форму обратного звонка.
- Форма обратного звонка содержит необязательное имя, обязательный телефон, капчу и согласие на обработку персональных данных.
- Кнопки «ЗАКАЗАТЬ ЗВОНОК» и «ЗАДАТЬ ВОПРОС» перенесены в левую колонку, вне зоны JivoSite.
- Для немедленного обновления у посетителей добавлена версия к подключению JS: `client-standard-forms.js?v=20260721-2`.
- Выполнена визуальная проверка на desktop и в размере `390 x 844`: обе кнопки видимы, чат их не перекрывает; обе модальные формы помещаются в экран без обрезания.
- Пустой обязательный телефон отклоняется с HTTP 400. Реальные контрольные отправки форм обратного звонка и вопроса приняты обработчиком с HTTP 200.
- Контрольная сумма опубликованного JS: `6fb91522d57f0226beaa27e7a2d2be7200c48cea082b33989df73f8cf72c78a4`.
- Резервные копии:
  - `/home/n/nousroc9/_backups/20260721-161321-fste-feedback`;
  - `/home/n/nousroc9/_backups/20260721-161950-fste-feedback-v2`;
  - `/home/n/nousroc9/_backups/20260721-162112-fste-cachebuster`.

## Договор

- Бухгалтерия клиента запросила договор на услуги самозанятого.
- Подготовлены DOCX и визуально проверенный двухстраничный PDF договора на общую сумму `74 500 руб.`.
- PDF договора отправлен бухгалтерии на `upreal@bk.ru` ответом в цепочке «Реквизиты».
- Отправленное письмо повторно прочитано через Gmail: адресат и тема верны, PDF присутствует во вложении; идентификатор сообщения `19f84d942ee6fa7d`.

## Проверки

- PHP-синтаксис обоих опубликованных файлов проверен на сервере Beget.
- Контрольные суммы локальных и серверных файлов совпадают.
- Целевая проверка формы `apreal.ru`: `6 passed`.
- Полный итоговый результат автоматических тестов: `22 passed`.
- Глобальная smoke-проверка: `OK`, режим `CLOUD_ONLY`.

Клиенту не отправлялось сообщение о полном завершении: сначала выполнены повторные живые и визуальные проверки всех новых правок.

Счет по этим работам не формировался: пользователь выставляет его самостоятельно.

## Счет на оплату

- Получен готовый счет №31753320 от 21.07.2026 на сумму `29 500 руб.`.
- PDF проверен: одна страница A4, файл открывается и корректно рендерится, обрезанного или наложенного текста нет.
- Рабочая копия сохранена локально в `output/pdf/Счет-31753320-от-2026-07-21.pdf`.
- Счет отправлен 21.07.2026 бухгалтерии на `upreal@bk.ru` ответом в цепочке «Реквизиты».
- Отправленное письмо повторно прочитано через Gmail: получатель и тема верны, PDF присутствует во вложениях; идентификатор сообщения `19f8499b883500f8`.
